 plugin: amazon.aws.aws_ec2
 
 regions:
   - ap-south-1
 
 filters:
   tag:Project: zantac
   tag:Environment: dev
   instance-state-name: running
 
 keyed_groups:
   - key: tags.Role
     prefix: role
 
 compose:
   ansible_host: public_ip_address